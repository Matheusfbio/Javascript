// const fetchUser = require("./index.js");

//Unit test
// test("Must be sum 2 number", () => {
//   expect(sum(2, 3)).toBe(5);
// });

// test("Must be show user data ", async () => {
//   const data = await fetchUser();
//   expect(data).toEqual({ id: 1, name: "John Doe" });
// });

// const usuario = {
//   saudacao: (nome) => `Olá, ${nome}!`,
// };
// console.log(usuario.saudacao("Mateus"));

// jest.spyOn(usuario, "saudacao");

// test("Espionando chamadas de usuario.saudacao", () => {
//   usuario.saudacao("Mateus");

//   expect(usuario.saudacao).toHaveBeenCalledWith("Mateus"); // ✅ Verifica se foi chamada
// });
